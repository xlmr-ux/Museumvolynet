{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm run build",
    "installCommand": "npm install",
    "startCommand": "npm run start"
  },
  "deploy": {
    "startCommand": "npm run start"
  }
}